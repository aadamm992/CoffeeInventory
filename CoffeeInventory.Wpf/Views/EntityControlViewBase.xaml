<UserControl x:Class="CoffeeInventory.Wpf.Views.EntityControlViewBase"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CoffeeInventory.Wpf.Views"
             mc:Ignorable="d"
             Background="#F0F0F0"
             d:DesignHeight="290"
             d:DesignWidth="700">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListView Margin="5"
                  Width="200"
                  FontSize="15"
                  SelectedItem="{Binding SelectedEntity, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  ItemsSource="{Binding EntityItemSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  DisplayMemberPath="{Binding EntityDisplayMemberPath, RelativeSource={RelativeSource AncestorType=UserControl}}" />

        <Grid Grid.Column="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ContentControl Grid.Row="0"
                            Content="{Binding EditorContent, RelativeSource={RelativeSource AncestorType=UserControl}}" />

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="_Update"
                        Width="100"
                        Margin="5"
                        FontSize="16"
                        Command="{Binding UpdateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />

                <Button Content="_Save As New"
                        Width="100"
                        Margin="0,5,0,5"
                        FontSize="16"
                        Command="{Binding SaveAsNewCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />

                <Button Content="_Delete"
                        Width="100"
                        Margin="5"
                        FontSize="16"
                        Command="{Binding DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />

            </StackPanel>

        </Grid>

    </Grid>

</UserControl>
