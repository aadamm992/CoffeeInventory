<UserControl x:Class="CoffeeInventory.Wpf.Views.ControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CoffeeInventory.Wpf.Views"
             xmlns:viewModels="clr-namespace:CoffeeInventory.Wpf.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:ControlsViewModel}"
             mc:Ignorable="d"
             Background="#F0F0F0"
             d:DesignHeight="310"
             d:DesignWidth="700">

    <Grid>

        <TabControl>

            <TabControl.Resources>

                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border"
                                        Background="White"
                                        BorderBrush="#AAA"
                                        BorderThickness="1,1,1,0"
                                        CornerRadius="3,3,0,0"
                                        Padding="0,3">
                                    <ContentPresenter ContentSource="Header"
                                                      HorizontalAlignment="Center" />
                                </Border>

                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected"
                                             Value="True">
                                        <Setter TargetName="Border"
                                                Property="Background"
                                                Value="#F0F0F0" />
                                        <Setter Property="Foreground"
                                                Value="Black" />
                                        <Setter Property="FontWeight"
                                                Value="Bold" />
                                    </Trigger>

                                    <Trigger Property="IsMouseOver"
                                             Value="True">
                                        <Setter TargetName="Border"
                                                Property="Background"
                                                Value="#BEE3FF" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

            </TabControl.Resources>

            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <UniformGrid IsItemsHost="True"
                                     Grid.Row="0"
                                     Rows="1"
                                     Columns="{Binding Path=Items.Count, RelativeSource={RelativeSource AncestorType=TabControl}}" />

                        <ContentPresenter Grid.Row="1"
                                          ContentSource="SelectedContent" />

                    </Grid>
                </ControlTemplate>
            </TabControl.Template>

            <!--Coffee Control View TabItem-->
            <TabItem Header="Coffee Control"
                     FontSize="16"
                     Validation.ErrorTemplate="{x:Null}"
                     Content="{Binding CoffeeControlViewModel}">
            </TabItem>
            
            <!--Brand Control View TabItem-->
            <TabItem Header="Brand Control"
                     FontSize="16"
                     Content="{Binding BrandControlViewModel}">
            </TabItem>
            
            <!--Capsule Type Control View TabItem-->
            <TabItem Header="Capsule Type Control"
                     FontSize="16"
                     Content="{Binding CapsuleTypeControlViewModel }">
            </TabItem>

            <!--Cup Size Control View TabItem-->
            <TabItem Header="Cup Size Control"
                     FontSize="16"
                     Content="{Binding CupSizeControlViewModel}">
            </TabItem>

        </TabControl>

    </Grid>

</UserControl>