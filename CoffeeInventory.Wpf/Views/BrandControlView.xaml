<UserControl x:Class="CoffeeInventory.Wpf.Views.BrandControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CoffeeInventory.Wpf.Views"
             xmlns:viewModels="clr-namespace:CoffeeInventory.Wpf.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:BrandControlViewModel}"
             mc:Ignorable="d"
             Background="#F0F0F0"
             d:DesignHeight="290"
             d:DesignWidth="700">

    <local:EntityControlViewBase EntityItemSource="{Binding CupSizesView}"
                                 SelectedEntity="{Binding SelectedBrand}"
                                 EntityDisplayMemberPath="Name"
                                 UpdateCommand="{Binding UpdateCommand}"
                                 SaveAsNewCommand="{Binding SaveAsNewCommand}"
                                 DeleteCommand="{Binding DeleteCommand}">

        <local:EntityControlViewBase.EditorContent>

            <Grid Margin="20">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Label Content="Name"
                       Margin="0"
                       FontSize="14"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center" />

                <TextBox Grid.Column="1"
                         Margin="10,0,0,0"
                         FontSize="14"
                         HorizontalAlignment="Left"
                         VerticalAlignment="Center"
                         Width="120"
                         Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            </Grid>

        </local:EntityControlViewBase.EditorContent>

    </local:EntityControlViewBase>

</UserControl>
